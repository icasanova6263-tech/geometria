[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "geometria"
version = "0.1.0"
description = "Paquete para calculos de geometria"
authors = [{name = "Ivanna Casanova"}]